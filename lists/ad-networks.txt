# bonkey-saftey :: ad-networks.txt
# Subscribe to this file in Pi-hole as a BLOCK list (Lists -> Add, type
# "Block list"):
#   https://raw.githubusercontent.com/Bonkey-Apps/Bonkey-Saftey/main/lists/ad-networks.txt
#
# Format: ABP syntax. ||example.com^ blocks example.com AND every subdomain,
# so a vendor spinning up a new host is covered without editing this file.
# Requires Pi-hole Core >= 5.16 / FTL >= 5.22. v6 qualifies.
#
# This replaces the old regex-deny.txt. Only the strict ||domain^ form is
# parsed -- no wildcards, no alternation, no @@ exceptions. Allowances live in
# lists/allowlist.txt, which is subscribed separately as an allow list.

##### Native-ad / content-recommendation vendors ###############################
||taboola.com^
||taboolasyndication.com^
||outbrain.com^
||engageya.com^
||revcontent.com^
||mgid.com^
||zemanta.com^

##### Mobile + in-app ad SDKs bundled into Store games #########################
||vungle.com^
||inmobi.com^
||adcolony.com^
||applovin.com^
||chartboost.com^
||ironsrc.com^
||supersonicads.com^
||unityads.unity3d.com^
||smaato.net^
||mopub.com^

##### Ad verification / viewability / fraud measurement ########################
# Pure ad-tech -- these exist only to measure and verify advertising, so
# whole-domain blocking is safe. Added after tpsc-uw1.doubleverify.com was seen
# getting through: the apex and the common subdomains were already blocked by
# exact-host entries, but the regional shard was not. This is the case ABP
# syntax exists for.
||doubleverify.com^
||moatads.com^
||adsafeprotected.com^
||adtrafficquality.google^

##### Exchanges / SSPs / bidders ###############################################
||adnxs.com^
||pubmatic.com^
||rubiconproject.com^
||openx.net^
||criteo.com^
||criteo.net^
||spotxchange.com^
||spotxcdn.com^
||adsrvr.org^
||3lift.com^
||sharethrough.com^
||teads.tv^

##### Microsoft / MSN ad brokers ###############################################
# These are the endpoints the Casual Games suite calls directly.
||mobileads.msn.com^
||srtb.msn.com^
||rad.msn.com^
||flex.msn.com^
||ads.msn.com^
||ads1.msn.com^
||ads2.msn.com^
||adbroker.mp.dse.microsoft.com^
||msmonplat.trafficmanager.net^

##### Found leaking through gravity -- Pi-hole log audit 2026-08-21 ##########
# Every entry below was queried between 2026-08-12 and 2026-08-20 and was
# ANSWERED with a real IP by this Pi-hole (verified host by host with
# `dig @127.0.0.1`, not inferred from the list files). None of them appear in
# the gravity table at all, so oisd/StevenBlack do not cover them either.
# Grouped by what the vendor is, so a future reader can judge each on its own.

# Cookie-sync / identity-resolution chains. These showed up as a cluster of
# near-identical hosts (tracenep.*, gtracenep.*, n-N-nycx.*), which is what a
# sync hop looks like -- exact-host entries would be obsolete within a week,
# so they are whole-domain.
||admaster.cc^
||bidprism.com^
||bidnest.cc^
||adworknow.com^
||csdata3.com^
||sptag2.com^
||ad-m.net^
||cookiesyncmtrk.com^
||meazy-ads.com^
||pmbmonetize.live^
||lunamedia.live^
||rtactivate.com^

# Exchanges / SSPs / bidders not already listed above.
||amx1.net^
||indexww.com^
||pokkt.com^
||fwmrm.net^
# Yahoo's bidder. Scoped to the ybp host -- ||yahoo.com^ would take out mail
# and search along with it.
||ybp.yahoo.com^
# Disqus sells its ad inventory through Zeta Global; ssp.disqus.com resolves
# into zetaglobal.io. Scoped to the ssp host so comment threads still load.
||ssp.disqus.com^

# Audience / DMP / measurement vendors.
||semasio.net^
||digitalaudience.io^
||ib-ibi.com^
||userreport.com^
||sundaysky.com^

# Creative serving.
||flashtalking.com^
# Taboola on its own registrable domain. ||taboola.com^ above does NOT cover
# this -- api-taboola.com is a separate domain, not a subdomain.
||api-taboola.com^

# DoubleVerify's newer domain. The ||doubleverify.com^ entry above caught the
# tpsc-* shards; cdn.dv.tech got through because it is a different domain.
||dv.tech^

# Amazon advertising. aax.amazon-adsystem.com and c.amazon-adsystem.com were
# already blocked as exact hosts, but the regional shards
# (aax-events-cell02-cf.us-east.3px.axp.*, aes.us-east.3px.*, sq-tungsten-ts.*)
# were not -- the same shard problem that motivated ||doubleverify.com^.
||amazon-adsystem.com^
# Amazon's A9 "tungsten" ad service on two further domains. Scoped to the ad
# subdomain on amazon.dev, which is shared with unrelated Amazon services.
||tq-tungsten.com^
||adsqtungsten.a9.amazon.dev^

# --- Not expressible here --------------------------------------------------
# ABP parsing has no wildcard form, so a broad casino word-match cannot live in
# a subscribed list. If casino creatives still get through after gambling-only
# has been active a few days, add this by hand under Domains -> Regex filter ->
# Deny. Note that broad matching on "bet" is deliberately avoided -- it collides
# with legitimate domains such as betterhelp.com.
#
#   (^|\.)(casino|slots|onlinecasino)[a-z0-9-]*\.(com|net)$
#
# Consent-management endpoints are likewise left out. Blocking them suppresses
# the GDPR/CCPA interstitial some ad units gate on, but can stall page loads on
# unrelated sites:
#
#   ||cookielaw.org^
#   ||cookiepro.com^
#   ||cookiebot.com^
